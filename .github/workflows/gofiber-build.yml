name: Docker Build
on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string
      new_tag:
        required: true
        type: string
      docker_username:
        required: true
        type: string
    secrets:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Docker Build
        run: |
          IMAGE="${{ inputs.docke_username }}/${{ inputs.app_name }}:${{ inputs.new_tag }}"
          docker build -f docker/gofiber/prod.Dockerfile -t "$IMAGE" .
